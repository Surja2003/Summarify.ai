# Optional: point the frontend at a deployed backend (Render) instead of relying only on local/in-browser summarization.
#
# Examples:
# VITE_BACKEND_URL=http://localhost:8000
# VITE_BACKEND_URL=https://your-render-service.onrender.com

VITE_BACKEND_URL=
