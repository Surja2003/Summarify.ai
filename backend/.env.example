# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=True

# CORS Settings
# Comma-separated list of allowed origins (e.g. "https://your-vercel-app.vercel.app,http://localhost:5173")
# Use "*" to allow all origins.
CORS_ORIGINS=*

# Cache Settings
MAX_CACHE_SIZE=100
MAX_HISTORY_SIZE=100

# Processing Settings
DEFAULT_SPEED_MODE=balanced
DEFAULT_DOMAIN=general
USE_ABSTRACTIVE=False

# Model Settings (for abstractive summarization)
TRANSFORMERS_CACHE=./models_cache
HF_MODEL=facebook/bart-large-cnn

# Optional: ChatGPT-like chat
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Optional: Hugging Face hosted inference fallback
HUGGINGFACE_API_TOKEN=
HUGGINGFACE_MODEL=HuggingFaceH4/zephyr-7b-beta
