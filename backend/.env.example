# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=True

# CORS Settings
CORS_ORIGINS=*

# Cache Settings
MAX_CACHE_SIZE=100
MAX_HISTORY_SIZE=100

# Processing Settings
DEFAULT_SPEED_MODE=balanced
DEFAULT_DOMAIN=general
USE_ABSTRACTIVE=False

# Model Settings (for abstractive summarization)
TRANSFORMERS_CACHE=./models_cache
HF_MODEL=facebook/bart-large-cnn
